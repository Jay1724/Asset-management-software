# Project Settings → API in the Supabase dashboard.
SUPABASE_URL="https://xxxxxxxxxxxx.supabase.co"
# The SERVICE ROLE key, not the anon key — this bypasses RLS and can create
# users, so keep it out of git and never put it in the frontend.
SUPABASE_SERVICE_ROLE_KEY="ey..."
